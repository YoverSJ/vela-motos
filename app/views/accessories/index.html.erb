<%= render "header_accessories" %>

<!--== Start Contact Area Wrapper ==-->
<section class="contact-area contact-page-area">
  <div class="container" style="padding-top: 40px">

    <p style="color: green"><%= notice %></p>

    <div class="tab-pane" id="orders" role="tabpanel" aria-labelledby="orders-tab">
      <div class="myaccount-content">
        
        <div class="row mb-3">
          <div class="col-md-8">
              <h3>Accesorios</h3>
          </div>
          <div class="col-md-4 d-flex justify-content-end">
              <%= link_to "Nuevo", new_accessory_path, class: "btn-theme" %>
          </div>
        </div>
        <div class="myaccount-table table-responsive text-center">
          <table class="table table-bordered">
            <thead class="thead-light">
              <tr>
                <th>#</th>
                <th>Nombre</th>
                <th>Garantía</th>
                <th>Precio Total</th>
                <th>Stock</th>
                <th>Acciones</th>
              </tr>
            </thead>
            <tbody>
              <% unless @accessories.blank? %>
                <% @accessories.each_with_index do |accessory, index| %>
                  <tr>
                    <td>
                      <%= (@pagy.page - 1) * @accessories.limit_value + index + 1 %>
                    </td>
                    <td><%= accessory.name %></td>
                    <td><%= accessory.warranty %></td>
                    <td><%= accessory.total_price %></td>
                    <td><%= accessory.stock %></td>
                    <td>
                      <%= link_to accessory, class: "check-btn sqr-btn" do %>
                        <i class="fa fa-eye"></i>
                      <% end %>  
                      <%= link_to edit_accessory_path(accessory), class: "check-btn sqr-btn" do %>
                        <i class="fa fa-pencil"></i>
                      <% end %>  
                      <%= link_to accessory, method: :delete, data: { confirm: '¿Estas seguro de eliminar este accesorio?' }, class: "check-btn sqr-btn" do %>
                        <i class="fa fa-trash"></i>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              <% else %>
                <tr>
                  <td colspan="6">No hay resultados</td>
                </tr>
              <% end %>
            </tbody>
          </table>
          <%# pagy_items_selector_js(@pagy) %>
          <%# render "pagy/custom_pagy_items_selector", pagy: @pagy, items: @items unless @accessories.blank? %>
        </div>
        <div class="d-flex justify-content-center">
          <%= render "pagy/custom_pagy_info", pagy: @pagy unless @accessories.blank? %>
        </div>
        <%= render "pagy/custom_pagy", pagy: @pagy unless @accessories.blank? %>
      </div>
    </div>

  </div>
</section>
<!--== End Contact Area Wrapper ==-->